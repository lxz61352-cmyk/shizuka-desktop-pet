# 更新公告

> 这里是「更新公告」的文本位置。
> 每次程序更新完、自动重启后，会把**刚更新到的那个版本**的公告弹一次。
> 写法：每个版本用一行 `## vX.Y.Z` 起一个小节，下面写「从上一个版本到这个版本改了什么」。
> 用户当前版本号 = `src/app_identity.py` 里的 `APP_VERSION`。

## v0.8.2（由大肥鱼代写）

1. 天气查不到时不再乱猜：气象局站点没有精确匹配就不报天气——你主动问会直说「没能确认城市」，开机问候则退化成不带天气的普通问候（以前会把同名地名的天气安到你头上）；定位源全部换成走 HTTPS 的免费源。
2. 微信跨天说「图3」时会说明给你的是哪一天的图，不再默默拿另一张图回答；已处理消息的记录改成按时间保留，减少很老的消息被重复执行的可能。
3. 删除待办会一并清掉它的备注和提醒记录，不会再弹出已删除事项的提醒。
4. 修复某些角色包在落地恢复动作时会让动态立绘整个失效、退回静态图片的问题；角色包里的互动区域或睡姿坐标写错时，启动阶段就会提示，而不是运行时崩掉。
5. 「电脑助手」带图片的任务即使选「继承 DSH 默认模型」也会强制用能看图的模型；任务过程窗口会显示本轮实际使用的模型。
6. 统一了回复开头的语气词清理规则；修复更新包路径里含 % 字符时更新脚本会出错。
7. 「双端共享记忆 / 立即同步记忆」还没做完，先和研究进展一样收起来（菜单里显示「开发中」，功能代码保留）。
8. 进行了一些优化和改进。

## v0.8.1（由大肥鱼代写）

1. 对话框、待办、对话记录、微信等窗口现在会跟着桌宠出现在同一块屏幕上（多显示器下不再跑到主屏，也不再闪一下）。
2. 模型名统一整理：DeepSeek 官方接口下各种模型名会归一到同一个；「电脑助手」窗口新增「任务模型」选择。
3. 「电脑助手」兼容新版接口、提问等待加了超时保护、带图片的任务会自动改用视觉模型，任务失败会留下日志方便排查。
4. 微信远程看图/讲题修复：现在真的能把图片给模型看；支持「图三」这类中文序号，没说图时自动带上最近的图片；截图如果就是桌宠自己刚说的话，不再自问自答。
5. 主动发言大幅降噪：减少「陪着你」这类套话，改成具体观察、实用提醒、小知识等更有内容的方向；重复或空泛的话不再说。
6. 修复完成待办后仍残留提醒、准点提醒不响、「晚上12点」时间算错，以及自动更新相关的问题。
7. 进行了一些优化和改进。

## v0.8.0

1. 对v0.7.4和v0.7.6合并过程中的大量问题进行了检查和修复。
2. 修复静香照抄历史回复的问题。
3. 修复剪贴板/截图重复回复的问题。
4. 「研究进展」功能还没做完，先暂时关掉。
5. 进行了大量优化和改进。

## v0.7.7（由大肥鱼代写）

1. 补回语音朗读（GPT-SoVITS）、背景音乐、窗口使用时长与时长日报、查询余额、开机自动启动、微信识图等 0.7.4 的功能。
2. 新增天气查询、新闻播报与检查更新；开机问候会结合真实天气。
3. 修复自然语言设待办失效、粘贴板与截图重复点评、输入框圆角不圆、语音服务卡死等问题；「更多设置」改为二级菜单并重排菜单顺序。
4. 进行了一些优化和改进。

## v0.7.6beta（由大肥鱼代写）

> （v0.7.5，v0.7.6均为乌贼优化版，不是正式版本；正式版直接到 v0.7.7。）

1. 重构为模块化结构，接入统一近期上下文（最多100条、48000字），并定期提炼长期事实与主题索引。
2. 待办大改：/待办 自动整理标题、备注与时间，分生活／研究，支持提前提醒、周期提醒，桌面和微信双通道提醒。
3. 新增 /电脑 文件助手与文献筛选；长对话可滚动复制，基础设置收进「更多设置」。
4. 进行了一些优化和改进。

## v0.7.4

1. 修改了api key的填入功能与逻辑，新增部分模型适配（该功能暂不稳定，可能无法适配部分模型接口，deepseek与glm已经过验证可以正常使用）
2. 优化了对话逻辑和说话方式。
3. 进行了一些优化和改进。
（说明：记忆系统与前后文关联目前还处于混乱状态，有可能会答非所问）

## v0.7.3

1. 改善了自动更新时由于连接不上git/速度较差导致的下载问题，并支持自动从镜像站下载。
2. 增加了查询token余额的功能（其实是在v0.7.2中因为不正确的指令导致功能被关掉了）。
3. 增加了GPT-SoVITS的自动/手动配置和相关功能。
4. 进行了一些优化和改进。

## v0.7.2

1. 重写了角色卡与提示词。
2. 天气系统新增国内源。
3. 进行了一些优化和改进。

## v0.7.1

1. 添加了微信远程控制的功能，支持文件编辑，识图，对话
2. 进行了一些优化和改进。

## v0.7.0

1. 修复了 i wanna 被 rickroll 夺舍的问题。
2. 添加了检测更新以及下载功能。
3. 进行了一些优化和改进。

## v0.6.16

你被骗了~这是一个滚木测试版本，所有功能都在0.7.0正式使用咯

## v0.6.0

喂喂喂，喂喂喂，听得到吗🦍📞
从0.6.0版本正式开始加入了更新公告系统哦
