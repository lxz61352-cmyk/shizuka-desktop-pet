# 更新公告

> 这里是「更新公告」的文本位置。
> 每次程序更新完、自动重启后，会把**刚更新到的那个版本**的公告弹一次。
> 写法：每个版本用一行 `## vX.Y.Z` 起一个小节，下面写「从上一个版本到这个版本改了什么」。
> 用户当前版本号 = `src/pet.py` 里的 `APP_VERSION`。

## v0.6.16

你被骗了~

## v0.6.0

- （示例占位）从这里开始写 0.6.0 相对上一版的更新内容。
